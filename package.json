{
  "name": "workspace",
  "version": "0.0.0",
  "description": "AI-powered resume analysis with intelligent job matching capabilities.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "@types/node": "^20.17.24"
  }
}
